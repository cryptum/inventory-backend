let path,fs,collectiveSlugFromUrl,report,reportAndThrowError;_f90‍.x([["fetchStats",()=>fetchStats],["fetchLogo",()=>fetchLogo],["fetchPkg",()=>fetchPkg]]);_f90‍.w("path",[["default",["path"],function(v){path=v}]]);_f90‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_f90‍.w("./transforms",[["collectiveSlugFromUrl",["collectiveSlugFromUrl"],function(v){collectiveSlugFromUrl=v}]]);_f90‍.w("./misc",[["report",["report"],function(v){report=v}],["reportAndThrowError",["reportAndThrowError"],function(v){reportAndThrowError=v}]]);




const fetchJson = async url => {
  try {
    return (await global.fetch(`${url}.json`)).json()
  } catch (e) {
    _f90‍.a("report",report)(e)
    _f90‍.a("reportAndThrowError",reportAndThrowError)(`Could not fetch ${url}.json`)
  }
}

       const fetchStats = async collectiveUrl => {
  try {
    return await fetchJson(collectiveUrl)
  } catch (e) {
    _f90‍.a("report",report)(e)
    _f90‍.a("report",report)(`Could not load the stats for ${_f90‍.a("collectiveSlugFromUrl",collectiveSlugFromUrl)(collectiveUrl)}`)
  }
};_f90‍.j(["fetchStats"]);

       const fetchLogo = async logoUrl => {
  if (!logoUrl) {
    // Silent return if no logo has been provided
    return
  }
  if (!logoUrl.match(/^https?:\/\//)) {
    _f90‍.a("reportAndThrowError",reportAndThrowError)(`Your logo URL isn't well-formatted - ${logoUrl}`)
  }

  try {
    const res = (await global.fetch(logoUrl))
    if (isLogoResponseWellFormatted(res)) {
      return res.text()
    }
    _f90‍.a("report",report)(`Error while fetching logo from ${logoUrl}. The response wasn't well-formatted`)
  } catch (e) {
    _f90‍.a("report",report)(`Error while fetching logo from ${logoUrl}`)
  }
};_f90‍.j(["fetchLogo"]);

const isLogoResponseWellFormatted = res => res.status === 200 && res.headers.get('content-type').match(/^text\/plain/)

       const fetchPkg = pathToPkg => {
  const fullPathToPkg = _f90‍.a("path",path).resolve(`${pathToPkg}/package.json`)
  try {
    return JSON.parse(_f90‍.a("fs",fs).readFileSync(fullPathToPkg, 'utf8'))
  } catch (e) {
    _f90‍.a("reportAndThrowError",reportAndThrowError)(`Could not find package.json at ${fullPathToPkg}`)
  }
};_f90‍.j(["fetchPkg"]);
